# Open Science URL Placeholder

Use the same URL below in both places:

- CCS submission form artifact URL field
- Open Science section of the paper

## Recommended URL

`https://github.com/<your-org-or-user>/freqllm-ccs26-artifact`

## Final Checklist

1. Push this directory to GitHub.
2. Verify that `bash scripts/reproduce_all.sh` works from a fresh checkout.
3. Confirm that the generated files in `figures/` and `tables/` match the paper.
4. Replace the placeholder URL above with the public repository URL.

## Scope

This artifact reproduces the published paper figures and tables from validated experiment snapshots and includes a public reference implementation snippet with `FreqMixer` terminology.
